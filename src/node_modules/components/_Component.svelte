<script>
  export let extended;
  export let variant = "sm";
  export { className as class };

  let className = "";
</script>

<style>
  .component {
    position: relative;
    display: flex;
    flex-direction: column;
    height: 100%;
  }
  .component .header :global(*) {
    color: var(--back);
    font-size: inherit;
    line-height: 1em;
  }
  .component .header {
    display: flex;
    padding-left: 40px;
    position: absolute;
    justify-content: center;
    flex-direction: column;
  }
  .component::before {
    content: "";
    display: flex;
    margin-top: 0px;
    background-color: rgb(179, 116, 1);
    background-color: var(--prime);
    position: relative;
  }
  :global(.xs.component::before),
  :global(.xs) .header {
    height: 50px;
    font-size: 0.8em;
  }
  :global(.sm.component::before),
  :global(.sm) .header {
    height: 80px;
    font-size: 1.3em;
  }
  :global(.md.component::before),
  :global(.md) .header {
    height: 100px;
    font-size: 1.6em;
  }
  :global(.lg.component::before),
  :global(.lg) .header {
    height: 130px;
    font-size: 2em;
  }
  :global(.xl.component::before),
  :global(.xl) .header {
    height: 150px;
    font-size: 3em;
  }
  .component .header ~ :global(*:nth-child(2)) {
    overflow: auto;
  }
  .component.extended .header ~ :global(*:nth-child(2)) {
    padding: 60px;
  }
</style>

<div class="component {className} {variant}" class:extended>
  <div class="header">
    <slot name="header">No Header provided</slot>
  </div>
  <slot />
</div>
