<script>
    import { goto, stores } from '@sapper/app';

    const { page } = stores();
    let className = '';

    export let segment;
    export {className as class};

    $: slug = $page.params && $page.params.slug ? `/ ${$page.params.slug}` : '';

</script>

<style>

    .breadcrumb {
		padding: 10px;
        background: var(--back);
        font-size: var(--code-fs);
        text-transform: uppercase;
        height: var(--breadcrumb-h);
	}

</style>

<div class="breadcrumb mb-1 {className}"><a href=".">home</a> / {segment} {slug}</div>