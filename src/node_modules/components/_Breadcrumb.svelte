<script>
  import { stores } from "@sapper/app";
  import { onMount } from "svelte";

  const { page } = stores();
  let className = "";

  export let segment;
  export { className as class };

  $: slug = $page.params.slug ? `${$page.params.slug}` : "";
</script>

<style>
  .breadcrumb {
    padding: 10px;
    background: var(--back);
    font-size: var(--code-fs);
    text-transform: uppercase;
    height: var(--breadcrumb-h);
  }
</style>

<div class="breadcrumb {className}">
  <a href=".">home</a>
  {#if slug}
    /
    <a rel="prefetch" href={segment}>{segment}</a>
    /
    {slug}
  {:else}/ {segment}{/if}
</div>
