import { writable } from "svelte/store";

function createStore() {

	const { subscribe, update } = writable(new Date(), () => { });

	setInterval()

	return {
		subscribe,
		update: (item) => update(val => item)
	}

}
export default timer = createStore();