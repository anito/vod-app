<script>
  import * as api from "api.js";
  import "./icon-button.scss";
  import { goto, stores } from "@sapper/app";
  import { onMount, getContext } from "svelte";
  import { createRedirectSlug } from "utils";
  import List from "@smui/list";
  import IconButton, { Icon } from "@smui/icon-button";
  import Button, { Label } from "@smui/button";
  import Fab, { Label as FabLabel, Icon as FabIcon } from "@smui/fab";
  import SimpleVideoCard from "components/SimpleVideoCard";
  import { videos } from "../../stores/videoStore";
  import { users } from "../../stores/userStore";
  import { flash } from "../../stores/flashStore";
  import { DateRangePicker } from "@sveltejs/site-kit/";
  import Component from "./_Component.svelte";
  import { Header } from "@sveltejs/site-kit";
  import {
    addHours,
    endOfWeek,
    startOfWeek,
    startOfDay,
    startOfMonth,
    endOfDay,
    formatISO9075,
    parseISO,
  } from "date-fns";
  import * as locales from "date-fns/locale";

  export let selectionUserId;

  const { page, session } = stores();
  const maxDate = undefined;
  const user = $session.user;
  const { getSnackbar, configAtts } = getContext("snackbar");

  let root;
  let userRole;
  let locale = locales.de;
  let dateFormat = "dd. MMM yyyy";
  let firstDayOfWeek = "monday";
  let snackbar;
  let code;
  let message;
  let filtered;
  let group;
  let currentUser;
  let selectionVideoId;
  let selectedVideo;
  let userVideos = [];
  let _userVideos;
  let ids = [];

  $: currentUserIndex =
    typeof selectionUserId === "number" &&
    ((id) => $users.findIndex((usr) => usr.id === id))(selectionUserId);
  $: userVideos = $users[currentUserIndex] && $users[currentUserIndex].videos;
  $: (() => (selectionVideoId = null))(selectionUserId);
  $: currentUser =
    (filtered = ((id) => $users.filter((usr) => usr.id === id))(
      selectionUserId
    )) &&
    filtered.length &&
    filtered[0];
  $: userRole =
    (group = ((usr) =>
      $session.groups &&
      $session.groups.find((group) => group.id == usr.group_id))(
      currentUser
    )) && group.name;
  $: joinData =
    (selectedVideo =
      currentUser &&
      currentUser.videos.find((v) => v.id === selectionVideoId)) &&
    selectedVideo._joinData;
  $: startDate =
    (joinData && joinData.start && parseISO(joinData.start)) ||
    startOfDay(new Date(0));
  $: endDate =
    (joinData && joinData.end && parseISO(joinData.end)) ||
    endOfDay(new Date(0));
  $: noneUserVideos = $videos.filter(
    (v) => !userVideos.find((uv) => v.id === uv.id)
  );
  $: root && root.classList.toggle("datapicker--open", selectionVideoId);

  onMount(() => {
    root = document.documentElement;
    root.classList.add("timemanager--open");

    snackbar = getSnackbar();
    return () => root.classList.remove("timemanager--open");
  });

  async function videoSelectedHandler(e) {
    let video = e.detail;
    if (video.id == selectionVideoId) {
      selectionVideoId = null;
    } else {
      selectionVideoId = video.id;
    }
  }

  function onApplyHandler({ detail }) {
    let startDate = detail.startDate;
    let endDate = detail.endDate;

    let startYear = startDate.getFullYear();
    let startMonth = startDate.getMonth();
    let startDay = startDate.getDate();
    let startHours = startDate.getHours();
    let startMinutes = startDate.getMinutes();
    let startSeconds = startDate.getSeconds();

    let endYear = endDate.getFullYear();
    let endMonth = endDate.getMonth();
    let endDay = endDate.getDate();
    let endHours = endDate.getHours();
    let endMinutes = endDate.getMinutes();
    let endSeconds = endDate.getSeconds();

    let isoStart = formatISO9075(
      new Date(
        startYear,
        startMonth,
        startDay,
        startHours,
        startMinutes,
        startSeconds
      )
    );
    let isoEnd = formatISO9075(
      new Date(endYear, endMonth, endDay, endHours, endMinutes, endSeconds)
    );

    saveTime(isoStart, isoEnd, selectionVideoId);
  }

  async function saveTime(start, end, id, e) {
    e && e.stopPropagation();

    let associated = userVideos
      .filter((v) => v.id != id)
      .map((v) => ({ id: v.id }));
    let data = {
      videos: [
        {
          id: id,
          _joinData: { start, end },
        },
        ...associated,
      ],
    };

    const res = await saveUser(data);

    if (res.success) {
      let video, videos, idx;

      video = currentUser.videos.find((v) => v.id === id);

      if (video) {
        video = Object.create(video);
        video._joinData = { ...video._joinData, start, end };

        idx = currentUser.videos.findIndex((v) => v.id == video.id);
        videos = currentUser.videos.slice();
        videos = [
          ...videos.slice(0, idx),
          { ...videos[idx], ...video },
          ...videos.slice(idx + 1),
        ];
        currentUser.videos = videos;
      }
      handleSuccess(res, currentUser);
    } else {
      handleError(res);
    }
  }

  async function toggle(video, e) {
    e && e.stopPropagation();

    let ids = getUserVideoIds(video);
    let isRemove = ids.length < userVideos.length;

    // confirm remove dialoge
    if (isRemove) {
      if (!confirm(`Remove Video "${video.title}" from Subscription?`)) return;
    }

    let data = { videos: { _ids: [...ids] } };

    const res = await saveUser(data);

    if (res.success) {
      if (isRemove && selectionVideoId === video.id) selectionVideoId = null;
      currentUser.videos = _userVideos.slice();
      handleSuccess(res, currentUser);
    } else {
      handleError(res);
    }
  }

  function getUserVideoIds(video) {
    let idx;
    if (-1 !== (idx = userVideos.findIndex((itm) => itm.id === video.id))) {
      // remove video from user videos
      _userVideos = [...userVideos.slice(0, idx), ...userVideos.slice(idx + 1)];
    } else {
      // add video to user videos
      _userVideos = [...userVideos, video];
    }
    ids = _userVideos.map((v) => v.id);

    return ids;
  }

  function saveUser(data) {
    return api.put(`users/${selectionUserId}`, data, user && user.token);
  }

  function handleSuccess(res, user) {
    snackbar.isOpen && snackbar.close();
    users.put(user);

    configAtts(res.data && res.data.message);
    snackbar.open();
  }

  function handleError(res) {
    let path;
    snackbar.isOpen && snackbar.close();

    message = (res.data && res.data.message) || res.statusText;
    code = (res.data && res.data.code) || res.status;

    if (400 <= code && code < 500) {
      configAtts(message);
      snackbar.open();
    } else {
      flash.update({ type: "error", message });
      path = `login${createRedirectSlug($page)}`;
      configAtts(message, path);
      snackbar.open();
    }
  }
</script>

<style>
  .grid-item {
    background: var(--back-grid-item);
  }
  .user-videos {
    grid-area: one;
    overflow: auto;
  }
  .time,
  .videos {
    grid-area: two;
    overflow: auto;
  }
  .time {
    background: #fff;
  }
  .no-user-selected {
    display: flex;
    justify-content: center;
    align-items: center;
  }
  :global(.item > label) {
    width: 100%;
  }
</style>

<div
  class="grid-item user-videos"
  class:no-user-selected={!currentUser}
  class:no-videos={!userVideos.length || userRole === 'Administrator'}>
  <Component>
    <div slot="header">
      <h2>Subscriptions <strong>{currentUser.name}</strong></h2>
    </div>
    {#if currentUser}
      {#if userRole !== 'Administrator' && userVideos.length}
        <List class="video-list" threeLine avatarList singleSelection>
          {#each userVideos as video (video.id)}
            <SimpleVideoCard
              threeLine
              class="user-video"
              on:videoSelected={videoSelectedHandler}
              on:videoSelected
              {video}
              {selectionUserId}
              bind:selectionVideoId
              emptyDateText="Not published"
              emphasizeEmptyDate>
              <IconButton
                class="delete-action-button delete"
                on:click={(e) => toggle(video, e)}>
                <Icon class="material-icons">remove_circle_outline</Icon>
              </IconButton>
              <Button
                color="primary"
                class="self-center"
                variant="outlined"
                style="min-width: 134px;">
                <Icon class="material-icons">
                  {selectionVideoId === video.id ? 'close' : 'schedule'}
                </Icon>
                <Label>
                  {selectionVideoId === video.id ? 'close' : 'schedule'}
                </Label>
              </Button>
            </SimpleVideoCard>
          {/each}
        </List>
      {:else if userRole === 'Administrator'}
        <div class="flex flex-1 flex-col justify-center self-center">
          <div class="">Admins have access to all Videos</div>
        </div>
      {:else}
        <div class="flex flex-1 flex-col justify-center self-center">
          <div class="">User has no Videos</div>
        </div>
      {/if}
    {/if}
  </Component>
</div>
{#if selectionVideoId}
  <div class="grid-item time">
    <DateRangePicker
      {locale}
      {firstDayOfWeek}
      {startDate}
      {endDate}
      {maxDate}
      {dateFormat}
      on:apply={onApplyHandler}
      disabled={!selectionVideoId}
      resetTrigger={selectionVideoId}
      weekGuides
      weekNumbers
      todayBtn
      twoPages
      resetViewBtn
      timePicker
      timePickerSeconds
      timePickerControls
      todayBtnText="Heute"
      cancelBtnText="Reset"
      applyBtnText="Anwenden"
      actionBtnClass="picker-btn mdc-button mdc-ripple-upgraded mdc-button--unelevated" />
  </div>
  <Fab
    disabled={!noneUserVideos.length}
    class="floating-fab"
    color="primary"
    on:click={() => (selectionVideoId = null)}
    extended>
    <FabLabel>Available Videos</FabLabel>
    <FabIcon class="material-icons">movie</FabIcon>
  </Fab>
{:else}
  <div class="grid-item videos" class:no-videos={!noneUserVideos.length}>
    <Component>
      <div slot="header">
        <h2>Available Videos</h2>
      </div>
      {#if noneUserVideos.length}
        <List class="video-list" twoLine avatarList singleSelection>
          {#each noneUserVideos as video (video.id)}
            <SimpleVideoCard
              let:unmanagable
              class="video"
              emptyDateText={video.description}
              disabled={userRole === 'Administrator'}
              {video}
              {selectionUserId}>
              <IconButton
                disabled={unmanagable}
                class="add-action-button add"
                color="primary"
                on:click={(e) => toggle(video, e)}>
                <Icon class="material-icons">add_circle_outline</Icon>
              </IconButton>
              <IconButton
                class="self-center"
                color="primary"
                on:click={async () => await goto(`videos/${video.id}`)}>
                <Icon class="material-icons">edit</Icon>
              </IconButton>
            </SimpleVideoCard>
          {/each}
        </List>
      {:else}
        <div class="user-video">No more videos available</div>
      {/if}
    </Component>
  </div>
{/if}
