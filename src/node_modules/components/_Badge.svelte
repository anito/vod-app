<script>
  export let size = 'medium';
  export let before;
  export let after;
  export let above;
  export let below;
  export let right;
  export { className as class };

  let className = '';
</script>

<span class="badge {size}" class:before class:right class:after class:below class:above>
  <div class="badge-content {className}">
    <slot />
  </div>
</span>

<style>
  .badge {
    position: absolute;
  }
  .badge-content {
    text-align: center;
    display: inline-block;
    border-radius: 50%;
    transition: transform 0.2s ease-in-out;
    overflow: hidden;
    white-space: nowrap;
    text-overflow: ellipsis;
    pointer-events: none;
  }
  .medium {
    width: 22px;
    height: 22px;
    line-height: 22px;
  }
  .above {
    top: -11px;
  }
  .after {
    right: -11px;
  }
  .right {
    right: 11px;
  }
  .flash {
    background: var(--flash);
    color: var(--back-white);
  }
</style>
